<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>au.adp.lima-k3s</string>

    <!-- `limactl start` is idempotent: it returns immediately if the instance is
         already running, and boots it if not. Combined with StartInterval below
         this acts as both a boot hook and a watchdog. -->
    <key>ProgramArguments</key>
    <array>
        <string>/opt/homebrew/bin/limactl</string>
        <string>start</string>
        <string>k3s</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <!-- Re-check every 5 minutes. If the VM died, this brings it back.
         KeepAlive is wrong here: limactl exits once the VM is up, and the VM
         itself runs under the lima hostagent, not under launchd. -->
    <key>StartInterval</key>
    <integer>300</integer>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/opt/homebrew/sbin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>

    <key>StandardOutPath</key>
    <string>/Users/admin/Library/Logs/lima-k3s.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/admin/Library/Logs/lima-k3s.log</string>

    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
